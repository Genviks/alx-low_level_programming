#include <stdio.h>

/**
 * main - main function
 *
 * Return: Return(0) on success
 */
int main(void)
{
	int s = 97, e = 122;
	while (s <= e)
	l{
		putchar(s);
		s++;
	}

	putchar('\n');

	return (0)
}
